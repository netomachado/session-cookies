<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %> 
<body>
  <header class='d-flex justify-content-between'>
    <p>Bem vinde <%= usuario.nome %></p>
    <a href="/logout">
      <button>Sair</button>
    </a>
  </header>
  <% for (let i = 0; i < 6; i++) { %>
    <div>
      <h1>Cai da escada ontem</h1>
      <div>
        <button>like</button>
        <button>dislike</button>
      </div>
    </div>
  <% } %>
</body>
</html>